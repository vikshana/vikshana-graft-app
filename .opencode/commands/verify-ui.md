---
description: Verify UI changes in the running Graft plugin via Chrome DevTools MCP
agent: build
---
Load the `verify-ui` skill (call the skill tool with name "verify-ui") and run the full UI verification loop for the Graft plugin against http://localhost:3000/a/vikshana-graft-app.

Scenario / focus for this run: $ARGUMENTS
